/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "images.pexels.com",
            "img.freepik.com",
            "storage.googleapis.com"
        ]
    }
}

module.exports = nextConfig
